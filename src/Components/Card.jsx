import styled from 'styled-components'
import { CardBackground } from './Colors';

const Card = styled.div`
  border-radius: 16px;
  margin: 20px;
  background-color: ${CardBackground};
  box-shadow: black;
`

export default Card;